---
phase: 01-build-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-03
files_modified:
  - assets/css/speekr-admin.css
  - assets/css/speekr-admin.min.css
  - assets/css/speekr.css
  - assets/css/speekr.min.css
  - assets/css/src/speekr-admin.scss
  - assets/css/src/speekr.scss
  - assets/js/speekr.js
autonomous: false

must_haves:
  truths:
    - "WordPress admin loads with no 404 errors for CSS or JS assets"
    - "Plugin icon font renders in the WordPress admin menu"
    - "WordPress frontend loads with no 404 errors for CSS assets on Speekr pages"
    - "Frontend icon font renders correctly (talk link icons visible)"
    - "`npm run start` enters watch mode and recompiles on file change without errors"
    - "Old compiled CSS files removed from assets/css/ after build/ confirmed working"
  artifacts:
    - path: "build/admin/index.css"
      provides: "Live compiled admin CSS (verified loading)"
    - path: "build/frontend/style.css"
      provides: "Live compiled frontend CSS (verified loading)"
  key_links:
    - from: "WordPress admin"
      to: "build/admin/index.css"
      via: "wp_enqueue_style in inc/admin/enqueues.php"
      pattern: "build/admin/index\\.css"
    - from: "Speekr frontend page"
      to: "build/frontend/style.css"
      via: "wp_enqueue_style in inc/front/enqueues.php"
      pattern: "build/frontend/style\\.css"
---

<objective>
Verify the migration in a live WordPress environment, then remove the now-redundant legacy compiled assets.

Purpose: Browser verification confirms compiled assets load without 404s and fonts render. Only after confirmation is it safe to delete the old assets/css/ compiled files. The src/ SCSS files in assets/ are also removed (source of truth is now src/).
Output: Confirmed working build pipeline; legacy compiled CSS and SCSS source files deleted from assets/.
</objective>

<execution_context>
@/Users/CRG/.claude/get-shit-done/workflows/execute-plan.md
@/Users/CRG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-build-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test watch mode</name>
  <files>(no files modified)</files>
  <action>
    Run `npm run start` and confirm it starts without errors and enters watch mode. After confirming it started (look for "webpack X.X.X compiled successfully" and "watching for changes"), stop it with Ctrl+C.

    If errors appear, diagnose: likely cause is a missing src/ file. Check `src/admin/index.js`, `src/admin/style.scss`, `src/frontend/style.scss` all exist.
  </action>
  <verify>`npm run start` outputs "compiled successfully" before being stopped. The SIGINT exit code after Ctrl+C is acceptable.</verify>
  <done>`npm run start` enters watch mode without compilation errors. webpack reports successful compilation of admin/index and frontend/style entries.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser verification — no 404s, fonts render</name>
  <action>
    Built in Plans 01-03:
    - @wordpress/scripts v31.5.0 installed and building
    - webpack.config.js with admin + frontend entry points
    - Compiled assets in build/admin/index.{css,js} and build/frontend/style.css
    - PHP enqueues in 3 files updated to point to build/
    - admin.min.css 404 bug fixed as side effect
    - SCRIPT_DEBUG toggle removed from frontend enqueue

    Steps to verify:
    1. Open WordPress admin (your local WP instance) while logged in.
    2. Open browser DevTools Network tab. Navigate to a Speekr admin page (Talks > Add New, or Speekr settings).
    3. Confirm: no 404 for CSS or JS. Speekr icon visible in admin sidebar. Purple/violet color scheme intact.
    4. Navigate to a frontend page that lists Talks.
    5. Confirm: no 404 for build/frontend/style.css. Talk link icons visible (small icons before YouTube, slides, etc. links) — confirms font path fix.
    6. If icons are missing but no 404, font paths in src/frontend/style.scss need adjustment — report the issue.
  </action>
  <verify>No 404s in browser Network tab on admin or frontend Speekr pages. Admin sidebar icon visible. Frontend talk link icons visible.</verify>
  <done>All assets load without 404. Admin icon font renders. Frontend icon font renders (talk link icons visible). User types "approved".</done>
</task>

<task type="auto">
  <name>Task 3: Remove legacy compiled assets and source files from assets/</name>
  <files>
    assets/css/speekr-admin.css,
    assets/css/speekr-admin.min.css,
    assets/css/speekr.css,
    assets/css/speekr.min.css,
    assets/css/src/speekr-admin.scss,
    assets/css/src/speekr.scss,
    assets/js/speekr.js
  </files>
  <action>
    Only run after Task 2 checkpoint is approved.

    Delete old compiled CSS files (superseded by build/):
    ```bash
    rm assets/css/speekr-admin.css
    rm assets/css/speekr-admin.min.css
    rm assets/css/speekr.css
    rm assets/css/speekr.min.css
    ```

    Delete SCSS source files from old location (source of truth is now src/):
    ```bash
    rm assets/css/src/speekr-admin.scss
    rm assets/css/src/speekr.scss
    rmdir assets/css/src/ 2>/dev/null || true
    ```

    Delete the empty frontend JS stub (3-line empty IIFE, never enqueued, no src/ equivalent needed):
    ```bash
    rm assets/js/speekr.js
    ```

    Do NOT delete:
    - `assets/js/speekr-admin.js` — original source; src/admin/index.js is the copy. Both kept for now.
    - `assets/fonts/` — fonts stay in place, referenced by src/frontend/style.scss via ../../assets/fonts/
    - `assets/css/` directory itself
    - `assets/img/` — untouched

    Run `npm run build` once more after cleanup to confirm the build still works without those files present.
  </action>
  <verify>
    1. `ls assets/css/speekr*.css 2>/dev/null` — returns nothing
    2. `ls assets/css/src/ 2>/dev/null` — fails or empty
    3. `ls assets/js/speekr.js 2>/dev/null` — returns nothing
    4. `ls assets/fonts/admin assets/fonts/front` — both directories still exist
    5. `npm run build` — exits 0
  </verify>
  <done>Old compiled CSS files and old SCSS source files deleted from assets/. Empty assets/css/src/ directory removed. assets/js/speekr.js removed. assets/fonts/ untouched. npm run build still exits 0 after cleanup.</done>
</task>

</tasks>

<verification>
- `npm run start` enters watch mode without errors
- Browser: no 404s on admin or frontend Speekr pages
- Browser: admin plugin icon visible in sidebar
- Browser: frontend talk link icons visible (font path fix confirmed)
- `ls assets/css/speekr*.css 2>/dev/null` returns nothing
- `ls assets/css/src/ 2>/dev/null` returns nothing or directory not found
- `npm run build` exits 0 after cleanup
</verification>

<success_criteria>
WordPress admin and frontend pages load all Speekr assets without 404s. Icon fonts render in both admin (inline PHP) and frontend (compiled CSS). npm run build and npm run start both work. Legacy compiled CSS and SCSS source files removed from assets/. build/ is the canonical source of compiled assets.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-foundation/01-04-SUMMARY.md` using the summary template.
</output>
