---
phase: 05-templates-and-compatibility
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-03-06T17:00:00.000Z
---

<current_state>
Executing Phase 5, Plan 05-04, Task 2 (human-verify checkpoint). Test A (FSE Site Editor) PASSED. Test B (classic theme) in progress — major improvements made, re-verification needed. Test C (shortcodes) mostly working. Tests D (developer hooks) not yet verified.
</current_state>

<completed_work>

- Plan 05-01: DONE — hooks + speekr_talk_output filter in all 5 render.php files
- Plan 05-02: DONE — 4 FSE .html templates + inc/front/templates.php
- Plan 05-03: DONE — 5 classic PHP templates + shortcodes.php
- Plan 05-04 Task 1: DONE — templates.php + shortcodes.php wired into Speekr.php; has_archive => true on Talks CPT
- Bug fix (commit 9d576bb): Speekr_Templates_Loader::$template_dir property typo
- Bug fix (commit 5ecb37f): shortcodes.php moved to includes_front()
- Bug fix (commit 22410cc): TWO Site Editor fixes — removed wp-edit-post CSS dep, added wp_is_block_theme() guard to speekr_template_include
- Bug fix (commit 3f9b885): enqueues.php WP_Post_Type::$ID warning on CPT archive pages
- Bug fix (commit a0d26e9): Hide Talk as Content meta box from Gutenberg; invert speekr-as-article logic
- Bug fix (commit 8a26a4b): Fix checkbox label wording for speekr-as-article
- Bug fix (commit 92cb9f3): Add links to all talk cards; reorder media image priority
- Test A (FSE Site Editor): PASSED
- Round 2 fixes (commit c2cc9d7):
  - speaker-profile/render.php: added is_singular('speekr_speaker') detection for auto-resolution
  - speaker-profile/render.php: headshot slideshow (stage + thumbnail strip) for 2+ headshots
  - speaker-profile block.json: showRider attribute (default false); rider hidden unless showRider=true
  - shortcodes.php: [speekr_profile speaker=ID show_rider=1 show_download=1]; [speekr_talks layout=grid|list]
  - enqueues.php: block CSS enqueued on all classic CPT page contexts (speaker, talks, conference)
  - single-talk/render.php: speaker card (headshot, name, bio) via _speekr_talk_speaker meta
  - single-conference.php: full redesign — banner, header meta dl, talk cards list, speaker wall
  - CSS: rem -> em across speaker-profile and single-talk stylesheets; conference single styles in frontend/style.scss
- Bug fix (commit latest): enqueues.php — speekr-main CSS now loads on conference+speaker CPT pages (was talks-only)

</completed_work>

<remaining_work>

## Complete Test B (classic theme) re-verification
After the enqueues fix, re-check:
1. Speaker profile URL — should now show profile (is_singular detection added)
2. /talks/ archive — same styles as shortcode (block CSS now enqueued)
3. Single talk — conference info + speaker card visible
4. Conference single — structured header, talk cards, speaker wall (CSS now loads via speekr-main)
5. No white screen or fatal errors

## Test C (shortcodes)
- [speekr_profile speaker="ID"] — use speaker ID to target specific speaker
- [speekr_profile show_rider="1"] — reveals Speaker Preferences section
- [speekr_talks layout="list" limit="3"] — NOTE: limit attr not yet implemented in shortcode
- [speekr_map] — should work

## Test D (developer hooks)
Add temporarily to functions.php:
```php
add_action('speekr_before_profile', function($id, $atts) {
    echo '<p data-hook-test="before-profile">Hook fired: speaker ' . $id . '</p>';
}, 10, 2);
```
Visit speaker profile page — confirm paragraph appears before profile block. Remove after.

## After all 4 tests pass
- Spawn fresh continuation agent with user_response="approved"
- Run gsd-verifier
- Mark Phase 5 complete in ROADMAP.md

## Known remaining gaps / future work
- [speekr_talks limit="3"] shortcode attribute not implemented (block has no limit attribute either)
- showRider block editor toggle needs a JS rebuild (attribute works via shortcode + block.json default)
- Talk duration/time slot meta doesn't exist yet (needed for conference single "ordered by time")
- speekr_conf_speakers array not always populated (falls back to talk's _speekr_talk_speaker)

</remaining_work>

<decisions_made>

- speekr-main CSS (frontend/style.scss) now loads on all Speekr CPT pages (talks, conference, speaker)
- Conference single template: banner image + header meta dl + talk cards + speaker wall
- Speaker profile: showRider=false by default; is_singular('speekr_speaker') auto-detects on CPT URL
- Headshot slideshow: JS inline in render.php (deduped), CSS in compiled stylesheet
- rem -> em: all new/modified CSS uses em units

</decisions_made>

<blockers>

None currently — enqueues fix should resolve the "naked" conference single template. Verify in browser.

</blockers>

<next_action>
1. Reload the conference single page — CSS should now load via speekr-main
2. Complete Test B verification
3. Run Tests C and D
4. If all pass, type "approved" to close Phase 5
</next_action>
