---
phase: 02-data-layer
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - inc/cpt/conferences.php
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "GET /wp-json/wp/v2/speekr_conference/{id} returns a meta object containing _speekr_conf_speakers as an array"
    - "POST /wp-json/wp/v2/speekr_conference/{id} with meta._speekr_conf_speakers=[42,99] saves and returns those IDs"
    - "useEntityProp('postType','speekr_conference','meta') in a block exposes _speekr_conf_speakers as a readable/writable array"
  artifacts:
    - path: "inc/cpt/conferences.php"
      provides: "register_post_meta() call for _speekr_conf_speakers inside speekr_register_conference_meta()"
      contains: "_speekr_conf_speakers"
  key_links:
    - from: "inc/cpt/conferences.php"
      to: "WordPress REST API"
      via: "register_post_meta with type=array and show_in_rest schema"
      pattern: "register_post_meta.*_speekr_conf_speakers"
---

<objective>
Add the _speekr_conf_speakers meta field to the Conference CPT so conference organizers can attach an array of Speaker Profile post IDs to a conference, and that array is accessible via the block editor and REST API.

Purpose: Phase 3 editor blocks need to call useEntityProp() to read and write the speakers array. The field must be registered in Phase 2 before any block code can consume it.
Output: inc/cpt/conferences.php with a 6th register_post_meta() call appended inside speekr_register_conference_meta().
</objective>

<execution_context>
@/Users/CRG/.claude/get-shit-done/workflows/execute-plan.md
@/Users/CRG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@inc/cpt/conferences.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register _speekr_conf_speakers meta field in speekr_register_conference_meta()</name>
  <files>inc/cpt/conferences.php</files>
  <action>
    Open inc/cpt/conferences.php. Inside the speekr_register_conference_meta() function, append a 6th register_post_meta() call directly after the closing parenthesis and semicolon of Field 5 (_speekr_conf_talk_ref), before the closing brace of the function.

    Add this block:

    ```php
	// Field 6 — Speaker references (array of speekr_speaker post IDs).
	register_post_meta( 'speekr_conference', '_speekr_conf_speakers', array(
		'single'       => true,
		'type'         => 'array',
		'show_in_rest' => array(
			'schema' => array(
				'type'  => 'array',
				'items' => array(
					'type' => 'integer',
				),
			),
		),
		'default'      => array(),
		'auth_callback' => function() { return current_user_can( 'edit_posts' ); },
	) );
    ```

    Use tabs for indentation to match the surrounding code. The function closing brace `}` and the `add_action` call below it must remain unchanged and in place.

    Do NOT touch any other field registration, the CPT registration function, or any other file.

    Note: `single => true` is required even for array meta when using show_in_rest with a schema — WordPress needs single=true to expose the value correctly via the REST API for useEntityProp(). The `default => array()` ensures the field is always present in the REST response (never null) so block code can safely spread or iterate it without a null check.
  </action>
  <verify>
    Run PHP lint:
    /Applications/MAMP/bin/php/php8.2.0/bin/php -l inc/cpt/conferences.php

    Must output "No syntax errors detected".

    Confirm the field registration exists:
    grep -n "_speekr_conf_speakers" inc/cpt/conferences.php

    Expected: at least two matches — the comment line and the register_post_meta key string — both inside speekr_register_conference_meta().

    Confirm the function still has exactly one add_action call:
    grep -c "add_action.*speekr_register_conference_meta" inc/cpt/conferences.php

    Expected: 1
  </verify>
  <done>
    inc/cpt/conferences.php passes PHP lint. speekr_register_conference_meta() contains the _speekr_conf_speakers registration with type=array, single=true, show_in_rest schema specifying integer items, and default=[]. The existing 5 fields and the add_action call are unchanged.
  </done>
</task>

</tasks>

<verification>
After the task completes, verify the gap is fully resolved:

1. PHP lint passes:
   /Applications/MAMP/bin/php/php8.2.0/bin/php -l inc/cpt/conferences.php
   Expected: "No syntax errors detected"

2. Field registered with correct args:
   grep -A 15 "_speekr_conf_speakers" inc/cpt/conferences.php
   Expected: type=array, single=true, show_in_rest with schema.items.type=integer, default=array()

3. No duplicate add_action:
   grep -c "add_action.*speekr_register_conference_meta" inc/cpt/conferences.php
   Expected: 1

4. All five original fields still present:
   grep -c "register_post_meta" inc/cpt/conferences.php
   Expected: 6 (five original + one new)
</verification>

<success_criteria>
- inc/cpt/conferences.php passes PHP lint with no syntax errors
- _speekr_conf_speakers is registered as type=array, single=true, show_in_rest with integer-items schema
- GET /wp-json/wp/v2/speekr_conference/{id} includes _speekr_conf_speakers in the meta object
- The field can be written via REST API and read back as an array of integers
- Phase 3 block editor code can call useEntityProp('postType', 'speekr_conference', 'meta') and access _speekr_conf_speakers
- The existing five conference meta fields (_speekr_conf_date, _speekr_conf_city, _speekr_conf_country, _speekr_conf_url, _speekr_conf_talk_ref) are unmodified
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer/02-05-SUMMARY.md`
</output>
