---
phase: 02-data-layer
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - inc/common/taxonomies.php
  - inc/classes/Speekr.php
  - inc/admin/custom-meta-boxes.php
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "GET /wp-json/wp/v2/speekr_topic returns 200 with taxonomy data (not 404)"
    - "Topics taxonomy is registered on both admin page loads and REST API requests"
    - "Block editor sidebar Topics panel can fetch and save terms via REST"
  artifacts:
    - path: "inc/common/taxonomies.php"
      provides: "speekr_register_topics_taxonomy() function, loaded in all bootstrap paths"
      contains: "register_taxonomy"
    - path: "inc/classes/Speekr.php"
      provides: "require_once for taxonomies.php inside includes()"
      contains: "inc/common/taxonomies.php"
    - path: "inc/admin/custom-meta-boxes.php"
      provides: "Meta box UI without the taxonomy registration function (moved out)"
  key_links:
    - from: "inc/classes/Speekr.php"
      to: "inc/common/taxonomies.php"
      via: "require_once in includes()"
      pattern: "require_once.*taxonomies\\.php"
    - from: "inc/common/taxonomies.php"
      to: "WordPress REST API"
      via: "register_taxonomy with show_in_rest: true on init hook"
      pattern: "register_taxonomy.*speekr_topic"
---

<objective>
Close the REST API gap for the Topics taxonomy by moving speekr_register_topics_taxonomy() from inc/admin/custom-meta-boxes.php into a new inc/common/taxonomies.php file that is loaded via Speekr::includes(), ensuring the taxonomy is registered on all request paths including REST API.

Purpose: The taxonomy function is correct but stranded in the admin-only bootstrap path. Moving it to the common path unblocks /wp-json/wp/v2/speekr_topic (currently 404) and enables the block editor to fetch and save Topics terms via REST.
Output: inc/common/taxonomies.php (new), Speekr.php updated (require_once added), custom-meta-boxes.php updated (function removed).
</objective>

<execution_context>
@/Users/CRG/.claude/get-shit-done/workflows/execute-plan.md
@/Users/CRG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@inc/classes/Speekr.php
@inc/admin/custom-meta-boxes.php
@inc/common/custom-posts.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract taxonomy registration to inc/common/taxonomies.php</name>
  <files>inc/common/taxonomies.php</files>
  <action>
    Create inc/common/taxonomies.php as a new file. It must contain:

    1. The ABSPATH guard at the top (matching the pattern used in all other inc/ files):
       ```php
       <?php
       if ( ! defined( 'ABSPATH' ) ) {
           die( 'Cheatin\' uh?' );
       }
       ```

    2. The complete speekr_register_topics_taxonomy() function copied verbatim from inc/admin/custom-meta-boxes.php lines 12-43. Copy the full function including its docblock comment. Do NOT alter any logic — the function is already correct (hierarchical: false, show_in_rest: true, uses speekr_get_cpt_slug(), has apply_filters hook).

    3. The add_action call on line 44 of custom-meta-boxes.php:
       ```php
       add_action( 'init', 'speekr_register_topics_taxonomy' );
       ```

    The file should contain ONLY these elements — no other functions, no meta box code.

    Then remove the speekr_register_topics_taxonomy() function and its add_action call from inc/admin/custom-meta-boxes.php (lines 6-44: the docblock, function definition, and add_action). The meta box file must not define this function at all after the change.

    Verify there is no duplicate function definition across the codebase after the move.
  </action>
  <verify>
    Run PHP lint on both files:
    /Applications/MAMP/bin/php/php8.2.0/bin/php -l inc/common/taxonomies.php
    /Applications/MAMP/bin/php/php8.2.0/bin/php -l inc/admin/custom-meta-boxes.php

    Both must output "No syntax errors detected".

    Confirm the function exists only in taxonomies.php:
    grep -rn "speekr_register_topics_taxonomy" inc/

    Expected: matches in inc/common/taxonomies.php only (function definition + add_action). Zero matches in inc/admin/custom-meta-boxes.php.
  </verify>
  <done>
    inc/common/taxonomies.php exists with the correct taxonomy registration function and add_action. inc/admin/custom-meta-boxes.php no longer defines speekr_register_topics_taxonomy(). Both files pass PHP lint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire taxonomies.php into Speekr::includes()</name>
  <files>inc/classes/Speekr.php</files>
  <action>
    Add a require_once for taxonomies.php inside the includes() method of inc/classes/Speekr.php.

    Insert it directly after the require_once for inc/common/custom-posts.php (currently line 83). The new line must go in includes() — NOT in includes_admin() or includes_front().

    The new line:
    ```php
    require_once( SPEEKR_DIRNAME . '/inc/common/taxonomies.php' );
    ```

    After the change, the includes() method should load:
    - inc/functions/debug.php
    - inc/common/custom-posts.php
    - inc/common/taxonomies.php   ← new
    - inc/cpt/speaker-profile.php
    - inc/cpt/conferences.php
    - (remaining files unchanged)

    Do NOT touch includes_admin() or any other method.
  </action>
  <verify>
    Run PHP lint:
    /Applications/MAMP/bin/php/php8.2.0/bin/php -l inc/classes/Speekr.php

    Must output "No syntax errors detected".

    Confirm placement inside includes() (not includes_admin()):
    grep -n "taxonomies" inc/classes/Speekr.php

    Expected: one match inside the includes() method body (approximately line 84-86 after insertion). Confirm the surrounding lines are other includes() entries (custom-posts.php before it, speaker-profile.php after it).
  </verify>
  <done>
    inc/classes/Speekr.php has require_once for taxonomies.php inside includes() between custom-posts.php and speaker-profile.php. File passes PHP lint. The taxonomy will now be registered on every request path including REST API.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full gap is resolved:

1. PHP lint passes on all three modified files:
   /Applications/MAMP/bin/php/php8.2.0/bin/php -l inc/common/taxonomies.php
   /Applications/MAMP/bin/php/php8.2.0/bin/php -l inc/admin/custom-meta-boxes.php
   /Applications/MAMP/bin/php/php8.2.0/bin/php -l inc/classes/Speekr.php

2. Function defined exactly once:
   grep -rn "function speekr_register_topics_taxonomy" inc/
   Expected: one match in inc/common/taxonomies.php only.

3. Bootstrap path is correct:
   grep -n "taxonomies" inc/classes/Speekr.php
   Expected: match inside includes(), not inside includes_admin().

4. No regressions in existing includes:
   grep -n "custom-meta-boxes" inc/classes/Speekr.php
   Expected: still present in includes_admin() at its original position.
</verification>

<success_criteria>
- inc/common/taxonomies.php exists and passes PHP lint
- speekr_register_topics_taxonomy() is defined only in taxonomies.php (not in custom-meta-boxes.php)
- Speekr::includes() loads taxonomies.php via require_once
- All three modified files pass PHP lint with no syntax errors
- The taxonomy registration fires on all request paths (includes() always runs; includes_admin() only runs for admin requests)
- /wp-json/wp/v2/speekr_topic will return 200 on a live WordPress environment (gap from VERIFICATION.md closed)
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer/02-04-SUMMARY.md`
</output>
