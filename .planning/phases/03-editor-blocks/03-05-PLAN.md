---
phase: 03-editor-blocks
plan: "05"
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Editing a Talk post shows 4 block editor sidebar panels: Summary, Media Links, Conference, Appears In — all fields save without page reload"
    - "Editing a Conference post shows 3 block editor sidebar panels: Conference Details, Talk Reference, Speakers — all fields save correctly"
    - "Editing a Speaker Profile post shows 4 block editor sidebar panels: Headshots, Bio, Social Links, Rider — all fields save correctly"
    - "Classic Talks meta boxes are absent from the Gutenberg block editor view"
    - "No block throws a console error on load in any of the three CPT edit screens"
  artifacts:
    - path: "build/blocks/talk-meta/index.js"
      provides: "Compiled Talk meta block script"
    - path: "build/blocks/conference-meta/index.js"
      provides: "Compiled Conference meta block script"
    - path: "build/blocks/speaker-profile-meta/index.js"
      provides: "Compiled Speaker Profile meta block script"
  key_links:
    - from: "inc/blocks/blocks.php"
      to: "build/blocks/*/index.js"
      via: "register_block_type() loads editorScript"
      pattern: "register_block_type"
    - from: "build/blocks/talk-meta/index.js"
      to: "speekr-summary meta"
      via: "Panel saves via REST PUT /wp/v2/talks/{id}"
      pattern: "speekr-summary"
---

<objective>
Human verification that all three CPT editor blocks work correctly in the live WordPress admin.

Purpose: This is the integration checkpoint — confirming all panels appear, fields save via REST, classic Talks meta boxes are hidden, and no console errors occur across all three CPT edit screens.

Output: Human confirmation that Phase 3 success criteria are met.
</objective>

<execution_context>
@/Users/CRG/.claude/get-shit-done/workflows/execute-plan.md
@/Users/CRG/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build and confirm all three blocks compiled</name>
  <files>(no files modified)</files>
  <action>
Run a final clean build to ensure all three blocks are compiled:

```bash
npm run build
```

Confirm all three block directories exist in build/:
```bash
ls build/blocks/talk-meta/
ls build/blocks/conference-meta/
ls build/blocks/speaker-profile-meta/
```

Each directory should contain: index.js and index.asset.php.

Run PHP lint on all modified PHP files:
```bash
/Applications/MAMP/bin/php/php8.2.0/bin/php -l inc/blocks/blocks.php
/Applications/MAMP/bin/php/php8.2.0/bin/php -l inc/classes/Speekr.php
/Applications/MAMP/bin/php/php8.2.0/bin/php -l inc/admin/custom-meta-boxes.php
```

If any file fails lint or any block directory is missing, diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
npm run build exits 0.
ls build/blocks/ shows talk-meta/, conference-meta/, speaker-profile-meta/ directories.
All three PHP lint commands output "No syntax errors detected".
  </verify>
  <done>
npm run build exits 0. All three block directories exist under build/blocks/. All PHP files pass lint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify all three CPT editor panels in WordPress admin</name>
  <action>
Built in Plans 03-01 through 03-04:
- PHP block registration in inc/blocks/blocks.php (speekr_register_blocks glob loop)
- Talk meta keys registered for REST (speekr-summary, speekr-conf, _speekr_media_youtube/vimeo/slides)
- Classic Talks meta boxes hidden via __back_compat_meta_box: true
- Talk meta block: 4 panels (Summary, Media Links, Conference, Appears In read-only)
- Conference meta block: 3 panels (Details, Talk Reference search, Speakers add/remove)
- Speaker Profile meta block: 4 panels (Headshots with media library, Bio, Social Links, Rider)

Steps to verify in your local WordPress admin (MAMP):

**Talk edit screen:**
1. Open or create a Talk post in the block editor
2. Confirm 4 custom panels appear: "Talk Summary", "Media Links", "Conference", "Appears In"
3. Confirm the classic meta boxes (Talk Summary, Media Links, About the Conference) are NOT visible
4. Type text in Summary → Save draft → Reload → confirm text persisted
5. Open browser console (F12) → confirm no red errors

**Conference edit screen:**
1. Open or create a Conference post in the block editor
2. Confirm 3 custom panels appear: "Conference Details", "Talk Reference", "Speakers"
3. Fill in City and Country → Save → Reload → confirm fields persisted
4. In Talk Reference: type 2+ characters → confirm matching Talks dropdown appears → select one → Save → Reload → confirm selection persisted
5. Open browser console → confirm no red errors

**Speaker Profile edit screen:**
1. Open or create a Speaker Profile post in the block editor
2. Confirm 4 custom panels appear: "Headshots", "Bio", "Social Links", "Rider"
3. Click "Add Headshot" → confirm WordPress media library opens → select an image → confirm it appears in the panel
4. Type in Short Bio → Save → Reload → confirm text persisted
5. In Social Links: select "LinkedIn" → enter a URL → click "Add" → Save → Reload → confirm it persisted
6. Fill in AV / Tech rider field → Save → Reload → confirm persisted
7. Open browser console → confirm no red errors

**Cross-CPT isolation:**
1. On the Conference edit screen: confirm Talk-specific panels (Talk Summary, Media Links, Conference) do NOT appear
2. On the Talk edit screen: confirm Conference-specific panels (Conference Details, Talk Reference, Speakers) do NOT appear
  </action>
  <verify>All four verification sequences above pass without errors. No console errors on any edit screen.</verify>
  <done>Human types "approved". All panels appear on correct CPTs, all fields save and persist, no console errors, classic Talks meta boxes hidden in Gutenberg, panels do not bleed across CPT screens.</done>
</task>

</tasks>

<verification>
Human verification confirms:
- All three CPTs show correct sidebar panels
- Fields save and persist across page reload
- No console errors on any edit screen
- Classic Talks meta boxes hidden in Gutenberg
- Panels don't bleed across CPT edit screens
</verification>

<success_criteria>
All four checks above pass with human approval. Phase 3 Editor Blocks complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-editor-blocks/03-05-SUMMARY.md`
</output>
